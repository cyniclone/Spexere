abstract class Entity {
  int hp;    // Hit points
  float x, y; // X and Y position of entity

  //Constructor
  Entity (int hp, float x, float y) {
    this.hp = hp;
    this.x = x;
    this.y = y;
  }
}

